import type { Metadata } from "next";
import "./globals.css";
import SiteHeader from "@/components/SiteHeader";
import Providers from "./providers";

export const metadata: Metadata = {
  title: "Kalahari Database",
  description: "Botswana–Kalahari research: papers, projects, people & places.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  const year = new Date().getFullYear();
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="min-h-dvh bg-[var(--bg)] text-[var(--text)] antialiased">
        <Providers>
          <SiteHeader />
          <main className="container mx-auto max-w-6xl px-6 py-8">{children}</main>
          <footer className="border-t mt-16 py-8 text-sm opacity-80">
            <div className="container mx-auto max-w-6xl px-6">
              © {year} Kalahari Database — Open access ethos
            </div>
          </footer>
        </Providers>
      </body>
    </html>
  );
}
