import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Kalahari Database",
  description: "Botswana–Kalahari research: papers, projects, people, places.",
};

export default function Home() {
  return (
    <main className="p-8">
      <h1 className="text-3xl font-bold tracking-tight">Kalahari Database</h1>
      <p className="mt-2 text-base text-gray-600">Welcome — build is back online.</p>
    </main>
  );
}
