import { Section } from "@/components/Section";

export default function ProjectsPage() {
  return (
    <Section title="Projects" lead="Active and archived initiatives.">
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {/* Project cards here */}
      </div>
    </Section>
  );
}
